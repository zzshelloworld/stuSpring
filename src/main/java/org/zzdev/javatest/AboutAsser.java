package org.zzdev.javatest;

import org.junit.Assert;

public class AboutAsser {
    public static void main(String[] args) {
        boolean flag = true;
        // assert 是用来在完全测试里面的 断言
        Assert.assertFalse(flag);
    }
}
